apiVersion: v1
kind: Service
metadata:
  name: demo-api-preview
  labels:
    app: demo-api
    rol: preview
spec:
  type: NodePort
  selector:
    app: demo-api
    slot: green          
  ports:
    - name: http
      port: 3000
      targetPort: http
      nodePort: 30091
