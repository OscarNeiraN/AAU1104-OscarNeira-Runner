apiVersion: v1
kind: Service
metadata:
  name: ${SERVICE_NAME}
  labels:
    app: ${APP_NAME}
    role: live
    version: blue
spec:
  type: NodePort
  selector:
    app: ${APP_NAME}
    version: blue
  ports:
    - name: http
      port: 3000
      targetPort: http
      nodePort: 30090
