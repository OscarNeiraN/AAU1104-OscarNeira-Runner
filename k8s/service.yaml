apiVersion: v1
kind: Service
metadata:
  name: demo-api
  labels:
    app: demo-api
    rol: live
spec:
  type: NodePort
  selector:
    app: demo-api
    slot: blue           
  ports:
    - name: http
      port: 3000
      targetPort: http    
      nodePort: 30090
